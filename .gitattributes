# Normalize line endings so Windows checkouts don't hand CRLF files to Linux
# containers. Shell scripts MUST stay LF or /bin/sh breaks ("Illegal option").
* text=auto eol=lf
*.sh text eol=lf
*.mjs text eol=lf
